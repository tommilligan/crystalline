{
  "name": "crystalline",
  "description": "Crystal Ball — a real-time collaborative team decision-making tool based on de Bono's Six Thinking Hats.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tommilligan/crystalline.git"
  },
  "homepage": "https://github.com/tommilligan/crystalline#readme",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,green \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "tsc -b && npm run build -w frontend",
    "build:static": "tsc -b frontend && npm run build -w frontend",
    "typecheck": "tsc -b",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "concurrently": "^9.1.0",
    "playwright": "^1.63.0",
    "typescript": "~6.0.2"
  }
}

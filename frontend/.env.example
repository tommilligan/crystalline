# Liveblocks public API key — https://liveblocks.io/dashboard/apiKeys
# A public key is safe to ship to the browser; it only allows joining rooms this app creates.
VITE_LIVEBLOCKS_PUBLIC_KEY=pk_your_key_here
